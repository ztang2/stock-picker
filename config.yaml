default_strategy: balanced
risk_free_rate: 0.045  # 4.5% annualized (used for Sharpe ratio calculation)

# Weights are defined ONLY in src/strategies.py — do not duplicate here

thresholds:
  min_market_cap: 2.0e9
  min_volume: 500000

filters:
  sectors: []
  industries: []
  min_market_cap: null
  max_market_cap: null
  exclude_tickers: []

include_midcap: true   # Include S&P 400 MidCap stocks in universe

cache_hours: 24
top_n: 20
